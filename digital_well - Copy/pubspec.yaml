name: digital_well
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Flutter plugins
  device_info_plus: ^11.3.3
  health_connect: ^0.0.0
  intl: ^0.20.2

  permission_handler: ^11.0.0
  # Core
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true