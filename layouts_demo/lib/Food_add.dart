import 'package:flutter/material.dart';

class Food_add extends StatelessWidget{
  const Food_add({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is the Add Screen',style: TextStyle(fontSize: 24),),

    );
  }
}