import 'package:flutter/material.dart';

class Food_menu extends StatelessWidget{
  const Food_menu({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is the Menu Screen',style: TextStyle(fontSize: 24),),

    );
  }
}